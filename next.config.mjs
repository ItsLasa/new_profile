/** @type {import('next').NextConfig} */
const nextConfig = {
    
    images : {
        domains : ['devscard.netlify.app', 'localhost', 'picsum.photos'] // <== Domain name
      }
};



export default nextConfig;
